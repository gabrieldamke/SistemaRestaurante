@page "{id:int}"
@model Web.Pages.Produto.Delete

@{
    Layout = "_Layout";
    ViewData["Title"] = "Excluir";
}

<h2>@ViewData["Title"]</h2>

<div class="card">
    <div class="card-header">
        <h4 class="card-title">Produto</h4>
    </div>
    <div class="card-body">
        @if (Model.Produto is not null)
        {
            <dl class="row">
                <dt class="col-sm-3">
                    <label asp-for="Produto.Id" class="form-label"></label>
                </dt>
                <dd class="col-sm-9">
                    @Model.Produto.Id
                </dd>
                <dt class="col-sm-3">
                    <label asp-for="Produto.Nome" class="form-label"></label>
                </dt>
                <dd class="col-sm-9">
                    @Model.Produto.Nome
                </dd>
                <dt class="col-sm-3">
                    <label asp-for="Produto.Descricao" class="form-label"></label>
                </dt>
                <dd class="col-sm-9">
                    @Model.Produto.Descricao
                </dd>
                <dt class="col-sm-3">
                    <label asp-for="Produto.Preco" class="form-label"></label>
                </dt>
                <dd class="col-sm-9">
                    @Model.Produto.Preco
                </dd>
                <dt class="col-sm-3">
                    <label asp-for="Produto.Categoria" class="form-label"></label>
                </dt>
                <dd class="col-sm-9">
                    @Model.Produto.Categoria?.Nome
                </dd>
            </dl>
        }
        else
        {
            <p>Nenhum produto encontrado.</p>
        }
    </div>
</div>

<div class="mt-3">
    @foreach (var error in Model.Errors)
    {
        <p class="text-danger">@error</p>
    }
</div>

<div class="mt-3 d-flex align-items-center">
    @if (Model.Produto is not null)
    {
        <form method="post" class="mr-2">
            <input type="hidden" asp-for="Produto.Id" />
            <button type="submit" class="btn btn-danger">Excluir</button>
        </form>
    }
    <a asp-page="Index" class="btn btn-secondary">Voltar</a>
</div>